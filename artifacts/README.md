# Artifacts Repository

This directory acts as the centralized repository for all reusable machine learning assets.
It securely stores:
- Final trained models (models/)
- Interrupted checkpoints (checkpoints/)
- Preprocessing and Scaling tools (preprocessors/, scalers/)
- Deployment-ready exports (exports/)

**NEVER** store raw experiment outputs, CSV datasets, or PNG plots here!
