# Database

This folder holds database files (e.g., SQLite) and related scripts. It is used for storing material properties, experiment logs, model metadata, and tracking training progress across different runs.