# Tests

Contains unit tests and integration tests for the project components. 

## Sample Data and Outputs
This directory also contains sample_data/ and sample_output/ corresponding to a full run for **Tungsten** (1D, 2D, 3D). These serve as reference materials to understand the expected input format and the generated outputs without needing to run a full experiment.