# ML Core

Contains the core machine learning components, base classes, and architectures that are extended by specific models in the src/ directory. It defines the foundational structures for building and training neural networks.